Given an array of integers, find the sum of its elements.

For example, if the array arr == [1,2,3] ,1+2+3 == 6 , so return 6.

 public static int simpleArraySum(List<Integer> ar,int count) {
        int sum =0;
        for(int i =0;i<count;i++){
            sum += ar.get(i);
        }
        return sum;
    }

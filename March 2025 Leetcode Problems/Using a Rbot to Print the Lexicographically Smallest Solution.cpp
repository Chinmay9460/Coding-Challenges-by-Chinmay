#include <bits/stdc++.h>
using namespace std;

class Solution {
    private:
    static constexpr uint MAXN = 100'000;
    static char st[MAXN];

    public:
    static string robotWithString(string &s) noexcept __attribute__((hot)) {
        const uint n = s.size();
    }
};

int main() {
}

